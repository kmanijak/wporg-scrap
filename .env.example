# Copy to .env and fill in. The actual .env is gitignored.

# Contact email used in the scraper's User-Agent header. Required.
# wp.org asks scrapers to be identifiable so admins can reach you if the
# crawl misbehaves.
WPORG_SCRAP_EMAIL=you@example.com
